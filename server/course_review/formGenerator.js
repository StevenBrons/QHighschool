const default_review = require("./default_review.json");

// create Google Form for specified group
exports.generateFormForGroup = ({ id, courseId, courseName, courseDescription, teacherName, studyTime }) => {

	console.log("Generating form for group " + courseName);
	// een api call naar Google form maken
	// link retreiven
	
	return "link";
}
